# Agent Instructions

- Before the user asks a question, do NOT modify any code and do NOT run any build commands (such as `npm run build` or `npm run local-build`).
- Do NOT generate any pages with Chinese URLs (or percent-encoded Chinese characters in URLs). All URLs must be pure ASCII.
- Do NOT update or modify any existing URL addresses.
